import threading

import Bubble
import visualizing as vl
import Source_array as arr

print(arr.array)

thread1 = threading.Thread(target = Bubble.Bubble, args = ())
thread_visual = threading.Thread(target = vl.)
